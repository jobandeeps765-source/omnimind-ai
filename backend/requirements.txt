fastapi==0.115.0
uvicorn[standard]==0.30.6
motor==3.5.1
pymongo==4.8.0
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.9
httpx==0.27.2
redis==5.0.8
python-dotenv==1.0.1

# LLM provider SDKs (optional at runtime -- app falls back to offline mode
# if a key is missing, but the packages must be installed for auto-detection
# to import cleanly)
openai==1.51.0
anthropic==0.36.0
google-generativeai==0.8.3

# RAG stack (used by rag_layer once documents are wired in -- see roadmap)
#chromadb==0.5.5
pypdf==4.3.1
python-docx==1.1.2
openpyxl==3.1.5
pandas==2.2.2
